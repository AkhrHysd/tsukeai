{
  "name": "tsukeai",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.28.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "biome": "biome check .",
    "check:deps": "node scripts/check-workspace-dependency-direction.mjs",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "check": "pnpm run biome && pnpm run check:deps && turbo run check --filter='!tsukeai'",
    "test": "pnpm --filter @tsukeai/web test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "workerd"
    ]
  }
}
