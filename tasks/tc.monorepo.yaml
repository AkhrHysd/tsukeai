id: tc.monorepo
title: "モノレポと Biome のルート整備"
description: "pnpm workspaces・Turborepo・Biome 単一。apps と packages の依存方向を Design どおり一方向に固定する。"
dependsOn: []
allowedPaths:
  - .
acceptanceCriteria:
  - "ルートで Biome が実行できワークスペースが解決する。"
status: done
retryCount: 1
maxRunRetries: 1
maxReviewRetries: 1
validationPolicy:
  skipStages:
    - test
lastValidation:
  passed: true
  stage: test
  failures: []
  logs: "test: skipped by task validationPolicy"
lastReview:
  decision: accept
  summary: "Root Biome scripts are present, pnpm resolves the root/apps/packages workspaces, and the added dependency-direction check covers app-to-app and package-to-app violations."
  requiredFixes: []
  riskNotes:
    - "Local `pnpm run biome` could not be fully verified because this worktree has an incomplete dependency install (`biome: command not found`), but package.json and pnpm-lock.yaml include @biomejs/biome."
  scopeViolations: []
lastReviewInputPath: /Users/akyrhysd/work/tsukeai/.orchestrator/review-inputs/tc.monorepo-2026-05-13T15-57-21-553Z.md
