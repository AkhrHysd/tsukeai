# Transform Architecture Decision

## Status

Accepted for MVP implementation.

## Context

MVP では、利用者が入力した素の文章をサーバ側で変換し、検証に通った 5-7-5 の投稿または 7-7 の返信だけを公開する。素の全文は公開しない。LLM 鍵は Workers Secrets に置き、LLM 呼び出しはクライアントから直接行わない。

Design では、障害時は新規公開フローを止め、既存の公開句の閲覧は可能なら維持する方針としている。また、リクエスト集中時のコストを抑えるため、キュー・上限・バックオフを運用境界に置く。

`tc.transform-arch-gate` は、変換実装の前に同期／非同期、失敗時 UX、再試行・冪等性、コスト上限を確定するためのゲートである。

## Decision

MVP の変換アーキテクチャは **非同期ジョブを正とし、短時間だけ同期的に完了を待つ**方式とする。

- **投稿・返信リクエスト**: API は認証済みユーザーから素の入力を受け取り、冪等キーに基づいて変換ジョブを作成または既存ジョブに合流する。
- **初回レスポンス**: 変換が短時間で完了した場合は作成済みの公開句を返す。完了しない場合は `queued` または `processing` のジョブ状態を返し、クライアントは状態を再取得する。
- **公開条件**: LLM 出力が形式検証と公開可能性の検証に通った場合だけ公開レコードを作る。検証に落ちた素入力または途中出力は公開しない。
- **同期性の境界**: ユーザー操作は「送信直後に必ず公開される」ことを保証しない。公開は変換ジョブの完了に従う。
- **失敗時 UX**: 変換失敗、タイムアウト、上限超過、検証失敗は公開失敗として表示し、利用者には再送信または入力修正を促す。既存の公開句一覧やスレッド閲覧は、変換基盤の障害中でも可能な限り維持する。
- **再試行**: 一時的な LLM/API 失敗はサーバ側で上限付きに再試行する。形式検証失敗、認可失敗、入力制約違反、コスト上限超過は自動再試行しない。
- **冪等性**: 投稿・返信の作成は、`user_id`、対象種別、対象親 ID、入力本文のハッシュ、クライアント発行の冪等キーを含む論理キーで重複作成を防ぐ。同じキーへの再送は同じジョブまたは同じ公開結果を返す。
- **コスト上限**: アカウント／セッション単位の時間窓上限、同時実行上限、LLM 呼び出し回数上限、入力長上限を置く。上限に達した場合は新規ジョブを作らず、明示的な上限超過エラーを返す。

ジョブ状態は少なくとも `queued`、`processing`、`succeeded`、`failed`、`rejected` を持つ。`failed` は一時障害やタイムアウト後の失敗、`rejected` は検証失敗や入力制約違反など再試行しても同じ結果になりやすい失敗を表す。

## Boundaries

- 本 ADR は変換パイプラインの運用契約を固定する。DB スキーマ、具体的なキュー実装、ジョブ実行基盤、ポーリング間隔、HTTP ステータスコードの最終値は実装タスク側で決める。
- LLM プロバイダ、モデル名、プロンプト全文、トークン単価の具体値はここでは固定しない。ただし、実装は本 ADR の上限・再試行・ログ境界に従う。
- 素入力の全文を長期ログに残さない。観測にはジョブ ID、状態、失敗種別、本文ハッシュ、処理時間、概算コストなど、公開やデバッグに必要な最小限の情報を使う。
- キャッシュはプライバシーと整合する範囲に限る。本文そのものを共有キャッシュキーやログに使わない。
- 管理者による手動再実行、返金、モデレーション審査キューは MVP の範囲外とする。

## Consequences

- LLM 遅延や一時障害をユーザー操作から切り離せるため、Workers のタイムアウトや外部依存の揺らぎに対して実装を安定させやすい。
- ジョブ状態 API と失敗表示が必要になるため、完全同期方式より UI と共有契約は増える。
- 冪等キーと本文ハッシュにより、ブラウザ再送やネットワーク再試行で同じ投稿・返信が重複公開されるリスクを下げられる。
- 検証失敗を公開しないため、形式不一致や安全でない出力が GT に出るリスクを抑えられる。一方で、利用者には失敗理由を過度に詳細化せず、修正可能な案内に留める必要がある。
- コスト上限により予期しない利用集中やループ再試行を抑制できるが、上限到達時は正当な利用者にも待機や再試行を求める UX になる。
